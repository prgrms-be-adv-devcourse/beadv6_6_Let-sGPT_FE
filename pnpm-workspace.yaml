# pnpm 11+ 는 빌드 스크립트 허용 목록을 이 파일의 allowBuilds 맵에서 읽는다.
# (postinstall 스크립트는 기본 차단 → 네이티브 빌드가 필요한 패키지만 명시적으로 허용)
allowBuilds:
  '@biomejs/biome': true
  '@tailwindcss/oxide': true
  '@swc/core': true
  esbuild: true
  lefthook: true
  msw: true
  unrs-resolver: true
