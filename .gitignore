# dependencies
node_modules/
.pnpm-store/

# build output
dist/
storybook-static/

# test / e2e artifacts
coverage/
playwright-report/
test-results/

# env (실제 값은 커밋하지 않음 — .env.example만 커밋)
.env
.env.*.local

# editor / OS
*.local
.DS_Store
.idea/

# Claude Code 개인 설정(공유 설정은 .claude/settings.json 만 커밋)
.claude/settings.local.json

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# NOTE: src/routeTree.gen.ts 는 자동생성이지만 타입체크 선행조건이라 의도적으로 커밋한다.

.serena