/** 전 화면 공통 하단 영역(앱 셸). */
export function SiteFooter() {
  return (
    <footer className="border-t">
      <div className="mx-auto max-w-6xl px-6 py-8 text-muted-foreground text-sm">
        <p>© 2026 Let'sGPT — 한정판 드롭 커머스</p>
      </div>
    </footer>
  );
}
